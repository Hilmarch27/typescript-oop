import { MathUtil } from "../src/math-util";

describe('namespace', () => {
    

    it('should work', () => {

        console.log(MathUtil.PI);
        console.log(MathUtil.sum(1, 2, 3, 4, 5));
    });

});
